import { PwContainerDirective } from './pw-container.directive';

describe('PwContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PwContainerDirective();
    expect(directive).toBeTruthy();
  });
});
