import { PwDragDropDirective } from './pw-drag-drop.directive';

describe('PwDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new PwDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
